# 🚀 Полная настройка автообновлений для Axom

## 1. Подключение к GitHub

### Создай репозиторий на GitHub:
1. Зайди на https://github.com
2. Нажми "New repository"
3. Название: `axom-player` (или любое другое)
4. Сделай **публичным** (для бесплатных GitHub Actions)
5. НЕ добавляй README, .gitignore, license

### Подключи локальный проект:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ТВО_ИМЯ/axom-player.git
git push -u origin main
```

## 2. Настройка секретов в GitHub

### Зайди в настройки репозитория:
1. Settings > Secrets and variables > Actions
2. Нажми "New repository secret"

### Добавь секреты:

**TAURI_PRIVATE_KEY:**
```
Скопируй содержимое файла: axom/~/.tauri/axom.key
```

**TAURI_KEY_PASSWORD:**
```
Пароль который ты вводил при генерации ключей
```

## 3. Обнови конфигурацию

В файле `src-tauri/tauri.conf.json` замени:
```json
"endpoints": [
  "https://github.com/ТВО_ИМЯ/axom-player/releases/latest/download/latest.json"
]
```

## 4. Создание релиза

### Обнови версию в файлах:
- `package.json` → `"version": "1.0.0"`
- `src-tauri/Cargo.toml` → `version = "1.0.0"`
- `src-tauri/tauri.conf.json` → `"version": "1.0.0"`

### Создай релиз:
```bash
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0
git push origin main
git push origin v1.0.0
```

## 5. Что происходит автоматически

1. **GitHub Actions** запускается при создании тега
2. **Собирает** приложение для Windows
3. **Подписывает** файлы твоим ключом
4. **Создает релиз** с установщиком и метаданными
5. **Пользователи** получают уведомление об обновлении

## 6. Для следующих обновлений

1. Внеси изменения в код
2. Обнови версию во всех файлах (например, 1.0.1)
3. Создай новый тег:
```bash
git add .
git commit -m "Release v1.0.1"
git tag v1.0.1
git push origin main
git push origin v1.0.1
```

## 🔑 Важные файлы

- **Приватный ключ**: `axom/~/.tauri/axom.key` (НИКОМУ НЕ ПОКАЗЫВАЙ!)
- **Публичный ключ**: уже в `tauri.conf.json`
- **Пароль**: тот что вводил при генерации

## ⚠️ ВНИМАНИЕ

- **НЕ ПОТЕРЯЙ** приватный ключ и пароль!
- **БЕЗ НИХ** автообновления работать не будут
- **СДЕЛАЙ БЭКАП** файла `axom/~/.tauri/axom.key`

## 🎯 Результат

После настройки:
- Пользователи скачивают установщик один раз
- При выходе новой версии получают уведомление
- Обновляются одним кликом
- Все происходит автоматически через GitHub!