# Полная инструкция по релизу Axom

## 🚀 Быстрый старт

### 1. Подготовка
```bash
# Очистка кода от комментариев
powershell -ExecutionPolicy Bypass -File clean-code.ps1

# Установка зависимостей
npm install

# Генерация ключей для автообновлений
powershell -ExecutionPolicy Bypass -File generate-keys.ps1
```

### 2. Настройка GitHub
1. Создайте репозиторий на GitHub
2. Добавьте секреты в Settings > Secrets:
   - `TAURI_PRIVATE_KEY` - приватный ключ из ~/.tauri/axom.key
   - `TAURI_KEY_PASSWORD` - пароль ключа (если есть)
3. Обновите URL в `tauri.conf.json`:
   ```json
   "endpoints": ["https://github.com/ВАШ_ЮЗЕР/ВАШ_РЕПО/releases/latest/download/latest.json"]
   ```

### 3. Сборка и релиз
```bash
# Локальная сборка для тестирования
npm run tauri:build

# Создание релиза через GitHub
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0
git push origin main
git push origin v1.0.0
```

## 🔒 Защита от декомпиляции

### Что уже настроено:
- ✅ Минификация и обфускация JavaScript/TypeScript
- ✅ Удаление всех console.log и отладочной информации
- ✅ Оптимизация Rust с максимальным уровнем (opt-level = 3)
- ✅ Link Time Optimization (LTO)
- ✅ Удаление отладочных символов (strip = true)
- ✅ Panic = abort для меньшего размера
- ✅ Удаление всех комментариев из кода

### Дополнительная защита:
- Rust код компилируется в нативный машинный код
- Используется статическая линковка
- Все строки и константы обфусцированы

## 🔄 Автообновления

### Как работает:
1. При запуске приложение проверяет GitHub Releases API
2. Сравнивает текущую версию с последней доступной
3. Показывает диалог обновления пользователю
4. Скачивает и устанавливает обновление автоматически

### Настройка версий:
- Обновите версию в `package.json`
- Обновите версию в `src-tauri/Cargo.toml`
- Обновите версию в `src-tauri/tauri.conf.json`
- Создайте git tag с версией

## 📦 Структура релиза

После сборки создаются:
- `axom_1.0.0_x64_en-US.msi` - установщик Windows
- `latest.json` - метаданные для автообновлений
- Подписанные файлы для безопасности

## 🛠 Команды для разработки

```bash
# Разработка
npm run tauri:dev

# Сборка для тестирования
npm run tauri:build

# Сборка для релиза
npm run tauri:build:release

# Очистка кода
powershell -ExecutionPolicy Bypass -File clean-code.ps1
```

## 📋 Чеклист перед релизом

- [ ] Код очищен от комментариев
- [ ] Версии обновлены во всех файлах
- [ ] Ключи подписи сгенерированы
- [ ] GitHub секреты настроены
- [ ] URL автообновлений корректный
- [ ] Локальная сборка работает
- [ ] Git tag создан и отправлен

## 🎯 Результат

После выполнения всех шагов:
- Пользователи получат защищенный exe файл
- Автообновления будут работать через GitHub
- Код будет максимально защищен от декомпиляции
- Размер приложения минимизирован